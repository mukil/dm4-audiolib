<project>

    <modelVersion>4.0.0</modelVersion>

    <name>DeepaMehta 3 Audi-0h-Canvas Model</name>
    <groupId>de.deepamehta</groupId>
    <artifactId>dm3-audio-canvas-model</artifactId>
    <version>1.0</version>
    <packaging>bundle</packaging>

    <parent>
        <groupId>de.deepamehta</groupId>
        <artifactId>deepamehta3-parent</artifactId>
        <version>${parent-version}</version>
    </parent>

    <dependencies>
        <dependency>
            <groupId>de.deepamehta</groupId>
            <artifactId>deepamehta3-core</artifactId>
            <version>${core-version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <extensions>true</extensions>
                <configuration>
                    <instructions>
                        <Export-Package>
                            de.deepamehta.plugins.multiclient.migrations
                        </Export-Package>
                        <Private-Package>
                            de.deepamehta.plugins.multiclient
                        </Private-Package>
                    </instructions>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
